(defpackage cl-pipe-test
  (:use :cl
        :cl-pipe
        :prove))
(in-package :cl-pipe-test)

;; NOTE: To run this test file, execute `(asdf:test-system :cl-pipe)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
